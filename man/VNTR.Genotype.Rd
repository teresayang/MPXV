% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/VNTRcaller.R
\name{VNTR.Genotype}
\alias{VNTR.Genotype}
\title{Genotyping Variable Number Tandem Repeats (VNTR) for the genome sequence of
monkeypox virus (MPXV)}
\usage{
VNTR.Genotype(
  data,
  vntr = vntr,
  match_s = match_s,
  mismatch_s = mismatch_s,
  regionStart = regionStart,
  regionEnd = regionEnd,
  baseonly = T,
  VNTRoutput = F,
  finder = F
)
}
\arguments{
\item{data}{sequences from a file in FASTA format}

\item{vntr}{sequence of the repetitive unit}

\item{match_s}{matching weight}

\item{mismatch_s}{mismatching penalty}

\item{regionStart}{start position of VNTR region}

\item{regionEnd}{end position of VNTR region}

\item{baseonly}{logical. If TRUE, only uses the letters in base alphabet
i.e. A,C,G,T. If FALSE, consider degenerate code in the analysis.}

\item{VNTRoutput}{logical. If TRUE, export output to .csv file.}

\item{finder}{logical. If TRUE, call function \code{\link{STR_finder}}.}
}
\value{
\item{ID}{name of sequence} \item{r}{the copy of tandem repeats}
\item{match}{the number of matches} \item{mismatch}{the number of
mismatches} \item{indel}{the number of indels} \item{score}{alignment score
of VNTR region for each query strain} \item{start_pos}{start position of the
VNTR region for each query strain}
}
\description{
The funciton \code{VNTR.Genotype} computes the copy of the variable number
tandem repeats.
}
\details{
%% ~~ If necessary, more details than the description above ~~
}
\note{
%% ~~further notes~~
}
\examples{

## load example
data(example)


## VNTR
vntr <- c("T","TATGATGGA","AT","ATATACATT")
regionStart <- c(132436,150542,173240,178413)
regionEnd <- c(133216,151501,173320,179244)

## parameter settings
baseonly = T
match_s = 2
mismatch_s = -5
VNTRoutput = F
finder = F

## computes the copy of the variable number tandem repeats
out <- VNTR.Genotype(data = MPXVseq, vntr = vntr,
                     regionStart = regionStart, regionEnd = regionEnd,
                     match_s = match_s, mismatch_s=mismatch_s,
                     baseonly = baseonly,VNTRoutput = VNTRoutput,
                     finder = finder)


}
\references{
%% ~put references to the literature/web site here ~

Pagès H, Aboyoun P, Gentleman R, DebRoy S (2022) \emph{Biostrings: Efficient
manipulation of biological strings}.  R package version 2.64.0,
\href{https://bioconductor.org/packages/Biostringshttps://bioconductor.org/packages/Biostrings}.
}
\seealso{
%% ~~objects to See Also as \code{\link{help}}, ~~~
}
\author{
%% ~~who you are~~
}
